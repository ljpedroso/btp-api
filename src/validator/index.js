const userValidator = require('@app/validator/userValidator')
const categoryValidator = require('@app/validator/categoryValidator')
const payeeValidator = require('@app/validator/payeeValidator')
const billValidator = require('@app/validator/billValidator')

module.exports = { userValidator, categoryValidator, payeeValidator, billValidator }
